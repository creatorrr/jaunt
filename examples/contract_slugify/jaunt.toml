version = 1

[paths]
source_roots = ["src"]
test_roots = ["tests"]

[llm]
provider = "openai"
model = "gpt-5.2"
api_key_env = "OPENAI_API_KEY"

[contract]
battery_dir = "tests/contract"
derive = ["examples", "errors"]
strength = true
